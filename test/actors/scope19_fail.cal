/* this should fail because "d" is no longer reacheable after the block is finished. */

actor scope18() bool Port1 ==> int Port2, int Port3:
  
  procedure my_proc(int a, int b)
  begin
    begin
    var
      int d
    do
      d := a;
    end
    
    print("d = " + d);
  end

  action ==>
  do
    my_proc (5, 3);
  end

end