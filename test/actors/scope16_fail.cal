/* this should fail because b cannot be reached outside the block. */

actor scope16() bool Port1 ==> int Port2:

  
  a: action Port1:[a] ==> [b]
  do
    begin
    var
      int b
    do
      b := a;
    end
  end

end