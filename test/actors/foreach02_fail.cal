/* should fail because i is already declared! */

actor foreach02() bool Port1 ==> int Port2, int Port3:
  
  int a := 0;
  
  procedure my_proc()
  var
    int i := 5
  begin
    foreach int i in Integers(0, 9)
    var
      int b = i
    do
      print("b = " + b);
      a := b;
    end
  end

end