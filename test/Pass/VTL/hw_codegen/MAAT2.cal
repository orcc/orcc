package hw_codegen;

actor MAAT2() ==> :

	List(type:int, size=10) values := [i : for int i in 0 .. 9];

	action ==>
	var
		int a = 18,
		int b = 23
	do
		if a > b then
			values[2] := values[3];
		else
			values[4] := a + b;
		end
	end

end