// XFAIL: in a procedure, a local variable may not override a parameter (they are in the same scope).

actor Actor () ==> :

  procedure X(int PARAM)
  var
    int PARAM
  begin
  end

end
