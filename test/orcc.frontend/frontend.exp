load_lib orcc.exp

set outputDir "output"

foreach actor [lsort [glob $srcdir/$subdir/*.cal]] {
  frontendOneActor $actor
}

foreach actor [lsort [glob $srcdir/$subdir/*.c]] {
  frontendOneActor $actor
}
