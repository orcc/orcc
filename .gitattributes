# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.java text
*.xtend text
*.ecore text
*.genmodel text
*.classpath text
*.project text
*.c text
*.h text
*.cal text
*.txt text
*.xdf text
*.xdfdiag text

# Specific configuration
# Some eclipse plugins set CRLF to MANIFEST.MF files when quickfix is used. To avoid
# changes in whole files, we set these files to always have CRLF line endings
MANIFEST.MF text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.gif binary